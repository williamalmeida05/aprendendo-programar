/*
    ARRAY - MÉTODOS
*/

    const students = ['João', 'Maria', 'Pedro', 'Carla', 'Bia', 'Mauricio']

    
    
    students.shift()
    console.log(students)


    // Push = adicionar novos itens aos arrays 
    // students.push('Will')

    // length = informar quantos itens contém no array
    // console.log(students.length)

    // sort = informa o array por ordem alfabética
    // console.log(students.sort())

    // delete = deletar um iten de detro do array, conforme o index informado
    // delete students [0]
    // console.log(students[1])
    
    // splice (unir / ligar) = retirar itens e também incluir itens conforme necessário
    // retirar = students.splice(1,2)
    //incluir = students.splice(1,0,'Will')

    // slice (fatiar) = permite que tire itens do arrays
    // const newStudents = students.slice(0,3)
    // necessário criar uma constante para que seja criado o novo array corretamente, somente com as informações que irá sair

    // pop = retira o último item do array
    // students.pop()

    // shift = retira o primeiro item do array
    // students.shift()